<Application x:Class="Shoes.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Shoes"
             StartupUri="Views/Autorization.xaml">
    <Application.Resources>
        <!-- Стили для текста -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Times New Roman"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontFamily" Value="Times New Roman"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
        </Style>
        
        <!-- Стиль для кнопки-->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#00FA9A"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
        </Style>
        
        <!-- Стили для контрастного фона (Border)-->
        <!--<Style TargetType="Border">
            <Setter Property="Background" Value="#7FFF00"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>-->

        <!-- Стиль для окна !!!ВАЖНО НЕ ЗАБЫТЬ ПРОПИСАТЬ ЕГО В СТИЛЕ САМОГО ОКНА ИНАЧЕ НЕ ПРИМЕНИТСЯ-->
        <Style x:Key="WindowStyle" TargetType="Window">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Width" Value="800"/>
            <Setter Property="MinWidth" Value="800"/>
            <Setter Property="Height" Value="450"/>
            <Setter Property="MinHeight" Value="450"/>
        </Style>
    </Application.Resources>
</Application>
